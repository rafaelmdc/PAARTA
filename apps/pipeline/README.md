# HomoRepeat Pipeline App

This app owns the Nextflow workflow, execution configs, and operator-facing scripts.

Key paths:
- `main.nf`
- `nextflow.config`
- `conf/`
- `modules/`
- `workflows/`
- `scripts/`

Shared Python logic is intentionally outside this app in `src/homorepeat/`.
